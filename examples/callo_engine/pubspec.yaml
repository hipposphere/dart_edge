name: callo_engine
description: SIP audio engine example that bridges phone calls to Gemini Live.
version: 0.2.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  dart_edge_sip:
    hosted: "https://registry.hippolabs.org/pub/internal-pub/"
  googleai_dart: ^6.2.0
