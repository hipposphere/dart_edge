name: dart_edge_benchmark_shelf_router
description: Shelf router benchmark target for Dart Edge comparisons.
publish_to: none
version: 0.2.0

environment:
  sdk: ">=3.13.0 <4.0.0"

resolution: workspace

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
