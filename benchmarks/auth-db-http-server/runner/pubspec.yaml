name: dart_edge_auth_db_benchmark_runner
description: Runner for the auth/db HTTP benchmark suite.
publish_to: none
version: 0.2.1

environment:
  sdk: ">=3.13.0 <4.0.0"

resolution: workspace

dependencies:
  dart_edge_auth_db_benchmark_shared: any
