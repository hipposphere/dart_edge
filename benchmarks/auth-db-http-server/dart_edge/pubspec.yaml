name: dart_edge_auth_db_benchmark_dart_edge
description: Dart Edge target for the auth/db HTTP benchmark suite.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  dart_edge: 0.1.0
  dart_edge_auth: 0.1.0
  dart_edge_sql: 0.1.0
  dart_edge_auth_db_benchmark_shared: 0.1.0
