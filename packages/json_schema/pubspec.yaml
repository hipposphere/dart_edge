name: json_schema
description: Typed, const-friendly JSON Schema contracts for Dart.
version: 0.1.0
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/hipposphere/dart_edge/tree/main/packages/json_schema

environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dev_dependencies:
  test: ^1.30.0
