name: dart_edge_bluetooth_protocol
description: Bluetooth GATT protocol contracts and explicit code emitters for Dart Edge.
version: 0.1.0
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/hipposphere/dart_edge/tree/main/packages/dart_edge_bluetooth_protocol

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  dart_style: ^3.1.8

dev_dependencies:
  test: ^1.30.0
