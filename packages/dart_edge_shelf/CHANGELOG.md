## 0.1.1

- Update `dart_edge_http_server` constraint for catch-all route support.

## 0.1.0

- Initial internal release with `Router.mountShelfHandler(...)` for mounting
  Shelf handlers on Dart Edge HTTP routers.
