name: dart_edge_docker
description: Docker image generation for Dart Edge workspaces.
version: 0.3.1
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/hipposphere/dart_edge/tree/main/packages/dart_edge_docker

executables:
  dart_edge_docker:

environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.30.0
