name: dart_edge_audio
description: Native-backed audio probing and conversion utilities for Dart Edge.
version: 0.1.0
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/flowhorn/dart_edge/tree/main/packages/dart_edge_audio

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  code_assets: ^1.0.0
  dart_edge_core: ^0.1.0
  ffi: ^2.2.0
  hooks: ^1.0.3
  native_toolchain_rust: ^1.0.3

dev_dependencies:
  ffigen: ^20.1.1
  test: ^1.30.0
