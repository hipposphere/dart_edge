/// Optional helper library for convenience APIs built on top of Dart Edge.
///
/// Keep app-facing helpers here when they wrap the runtime without redefining
/// its core contracts.
library dart_edge_helpers;

export 'src/open_api_helpers.dart';
