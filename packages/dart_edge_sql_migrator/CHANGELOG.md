## 0.3.5

- Update `dart_edge_sql` constraint for prebuilt native asset support.

## 0.3.2

- Default migration metadata table name to `migrations`.
- Add PostgreSQL `tableSchema` support for the migration metadata table,
  defaulting to `dart_edge`.
- Add single-file Dart data-asset migration manifests and manifest-backed
  migration loading.

## 0.3.0

- Declare internal Dart Edge dependencies with the internal hosted registry.

## 0.2.0

- Update package constraints for the native HTTP routing and shared core API changes.

## 0.1.0

- Initial internal release.
