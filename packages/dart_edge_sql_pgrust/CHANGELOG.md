# 0.1.0

- Add temporary and persistent managed pgrust database endpoints.
- Discover pgrust and PostgreSQL `initdb` from explicit paths, environment
  variables, or `PATH`.
- Capture child-process logs and shut down owned pgrust processes with their
  Dart SQL pools.
