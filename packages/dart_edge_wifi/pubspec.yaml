name: dart_edge_wifi
description: Linux and macOS Wi-Fi network discovery and connection helpers for Dart Edge.
version: 0.2.0
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/hipposphere/dart_edge/tree/main/packages/dart_edge_wifi

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  ffi: ^2.2.0
  objective_c: ^9.3.0

dev_dependencies:
  ffigen: ^20.1.1
  test: ^1.30.0
