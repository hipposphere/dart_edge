name: dart_edge_codegen
description: Codegen annotations and generator-facing APIs for Dart Edge.
version: 0.1.0

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  dart_edge_http_server_runtime: 0.1.0

dev_dependencies:
  test: ^1.30.0
