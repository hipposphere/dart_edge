## 0.1.1

- Preserve response bytes from `package:http` for generated clients.
- Require `dart_edge_core` 0.3.10.

## 0.1.0

- Initial internal release with HTTP and WebSocket transports for generated
  Dart Edge clients.
