## 0.1.5

- Add the shared pointer-backed `NativeBinaryPayloadLease` implementation for
  native Dart Edge packages.
- Require `dart_edge_core` 0.3.41 for the platform-neutral lease contract.

## 0.1.4

- Add the shared `NativeByteStreamHandle` ownership and transfer contract.

## 0.1.2

- Add `NativeCompletionPort` for reusable native job completion notifications
  from Rust worker pools back to Dart.

## 0.1.0

- Initial shared FFI bridge package for Dart Edge native packages.
