name: dart_edge_native_bridge
description: Shared Dart FFI value types and helpers for Dart Edge native packages.
version: 0.1.5
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/hipposphere/dart_edge/tree/main/packages/dart_edge_native_bridge

environment:
  sdk: ">=3.13.0 <4.0.0"

resolution: workspace

dependencies:
  dart_edge_core:
    hosted: "https://registry.hippolabs.org/pub/internal-pub/"
    version: ^0.3.41
  ffi: ^2.2.0

dev_dependencies:
  ffigen: ^20.1.1
  test: ^1.30.0
